# Enable mini UART
enable_uart=1
# Run in 64-bit mode
arm_64bit=1
#dtparam=i2c_arm=on
#dtparam=spi=on
#dtoverlay=uart2-pi5
#dtparam=audio=on
#dtoverlay=vc4-kms-v3d
max_framebuffers=2
disable_overscan=1
arm_boost=1
usb_max_current_enable=1

# Use Das U-Boot
kernel=u-boot.bin
