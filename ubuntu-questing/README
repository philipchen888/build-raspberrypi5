Build ubuntu-questing sdcard for raspberrypi5:

Input files: ../rootfs-ubuntu-questing/linaro-esp.img
             ../rootfs-ubuntu-questing/linaro-rootfs.img

cd ~/build-raspberrypi5/ubuntu-questing

Plugin sdcard/usb3 stick to PC.
sudo ./mksdcardimg.sh
sudo mount /dev/sda1 /mnt
sudo 7z x ~/Downloads/RPi5_UEFI_Release.zip -o/mnt && sync && sudo umount /mnt
Eject sdcard/usb3 stick from PC.

Plugin sdcard/usb3 stick to rpi5.
Login:
Username: linaro
Password: linaro
Setup wifi. Disable power saving, auto suspend and screen saver.
uname -a
lsb_release -a
sudo ifconfig
sudo dpkg-reconfigure tzdata
sudo dpkg-reconfigure locales
date
sudo apt update
sudo apt upgrade
sudo apt dist-upgrade
sync
free
sudo shutdown -h now
Power off, power on

Check CPU speed:
cat /sys/devices/system/cpu/cpu0/cpufreq/scaling_cur_freq

Check rpi5 fan:
cat /proc/device-tree/cooling_fan/status
cat /sys/class/thermal/cooling_device0/type

glxinfo | grep OpenGL
date
Check if Ubuntu is running UEFI mode:
ls /sys/firmware/efi
Check display mode: ( wayland or x11 )
echo $XDG_SESSION_TYPE
Check audio driver: ( pulseaudio or pipewire )
pactl info

sudo apt install speedtest-cli python3-pip python3-setuptools ffmpeg mpv glmark2-es2-wayland pandoc vlc vlc-plugin-video-output vsftpd v4l-utils

speedtest-cli --secure

glmark2-es2-wayland

aplay -l
pavucontrol  ( select hdmi as default )

sudo add-apt-repository ppa:mozillateam/ppa
sudo apt update; sudo apt upgrade -y;
sudo apt install firefox
firefox -> youtube.com -> search YmDNhi07_Ho

Do follows on PC. Then ftp swan.mp4 to rpi5. (
git clone https://github.com/yt-dlp/yt-dlp.git
cd yt-dlp
make
cd ../
~/yt-dlp/yt-dlp -f 'bestvideo[ext=mp4][protocol=https]+bestaudio[ext=m4a][protocol=https]' 3qrKjywjo7Q -o swan.mp4 --postprocessor-args " -c:v libx265 -b:v 2M -c:a copy"
)

mpv --vo=gpu --hwdec=v4l2m2m swan.mp4
ffplay -autoexit swan.mp4
cvlc --gl=wgl --glconv=any swan.mp4

Plugin ASUS USB-BT500 bluetooth usb dongle to rpi5.
sudo rfkill list
hciconfig hci0
hcitool dev
hciconfig -a
